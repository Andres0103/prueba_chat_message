version: "3.9"

services:
  api:
    build: .
    container_name: chat-message-api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - ENVIRONMENT=docker
    restart: unless-stopped
